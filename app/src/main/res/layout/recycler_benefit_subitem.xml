<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
                name="title"
                type="String" />

        <variable
                name="content"
                type="String" />

    </data>

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="19dp"
            android:paddingLeft="19dp"
            android:paddingRight="19dp"
            android:paddingEnd="19dp"
            android:paddingVertical="8dp">

        <TextView
                android:id="@+id/benefitSubitemTitle"
                style="@style/benefit_subitem_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@{title}"
                tools:text="Código de licencia" />

        <TextView
                android:id="@+id/benefitSubitemContent"
                style="@style/benefit_subitem_content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:ellipsize="end"
                android:maxLines="3"
                android:text="@{content}"
                tools:text="SK3-8477-6083-6678-0859-7453" />

    </LinearLayout>


</layout>